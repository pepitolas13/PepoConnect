name: pepo_core
description: PepoConnect core - identity, pairing, discovery, transport, transfer engine and media pipeline (pure Dart).
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.13.0

dependencies:
  async: ^2.13.0
  collection: ^1.19.0
  crypto: ^3.0.6
  logging: ^1.3.0
  meta: ^1.16.0
  path: ^1.9.0
  pointycastle: ^4.0.0
  xxh3: ^1.2.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0

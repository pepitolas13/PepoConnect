tags:
  perf:
    description: Throughput measurements (slow, skipped in CI with --exclude-tags perf).
  native:
    description: Needs the Rust fast lane library (cargo build --release in packages/pepo_native/rust).
